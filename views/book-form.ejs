<%- include("partials/header.ejs") %>

<h1><%= locals.currentBook ? "Edit Book" : "Add Book" %></h1>
<form action="<%= locals.currentBook ? `/edit/${currentBook.id}` : `/add`%>" method="POST">

</form>

<%- include("partials/footer.ejs") %>